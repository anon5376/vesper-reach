{
  "name": "vesper-reach",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Vesper Reach — a retro paperback space survey game.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort",
    "verify-data": "node scripts/verify-data.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "playwright": "^1.55.0",
    "vite": "^6.3.5"
  }
}
